#include "chat.h"

void free_chat_items(chat_item_t **chats)
{
    // if (chats != NULL)
    // {
    //     for (int i = 0; chats[i]; i++)
    //     {
    //         // free(chats[i]->event_box_contact);
    //         // free(chats[i]->contact_container);
    //         // free(chats[i]->time_last_message);
    //         // free(chats[i]->login_last_message);
    //         // free(chats[i]->text_last_message);
    //         // free(chats[i]->contact_name_lable);
    //         // if (chats[i]->user_is_online != NULL)
    //         // {
    //         //     free(chats[i]->user_is_online);
    //         //     free(chats[i]->user_is_online_round);
    //         // }
    //         // free(chats[i]->contact_info);
    //         // free(chats[i]->contact_last_msg);
    //         // free(chats[i]->contact_name_container);
    //         // free(chats[i]->img_contact);
    //         // free(chats[i]->contact_img_container);
    //         // free(chats[i]);
    //     }
    //     free(chats);
    //     chats = NULL;
    // }
}